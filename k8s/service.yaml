apiVersion: v1
kind: Service
metadata:
  name: auth-sso-service
  labels:
    app: auth-sso-service
spec:
  selector:
    app: auth-sso-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: LoadBalancer